# CLAUDE.md

个人股票 AI 追踪 Agent —— 按配置的 A股/港股代码，**每周五收盘后（周跟踪）**自动生成追踪报告。

> 详细需求见 `docs/requirements.md`。本文件是给 Claude Code 的工作指引，需求变更时同步更新此文件。

## 开发方法：TDD（强制）

本项目采用测试驱动开发，**红-绿-重构**循环，严格遵守：

1. **先写测试**：任何生产代码前，先写一个会失败的单元测试（Red）。
2. **最小实现**：只写让该测试通过的最少代码（Green）。
3. **重构**：在测试保护下清理代码（Refactor）。
4. 未看到测试失败过，不认为测试有效。
5. 提交前 `dotnet test` 必须全绿。

**测试要点**：
- 数据源等外部 HTTP 依赖必须通过接口抽象 + Mock 测试，**单元测试不发真实网络请求**。
- 用真实抓取的接口响应样本存为测试 fixture（`tests/**/Fixtures/`），驱动解析器测试。
- 指标计算（MA、涨跌幅、量比、阈值判定、关键词命中）是纯函数，优先覆盖，边界用例齐全。
- 测试框架：xUnit + FluentAssertions + NSubstitute（Mock）。

## 技术栈

- **.NET 10 (LTS)**，Console App
- `HttpClient` + `System.Text.Json` 采集数据（无 akshare/efinance 等 Python 库等价物，直接调公开 HTTP 接口自行解析）
- `appsettings.json` 配置（`Microsoft.Extensions.Configuration` 绑定强类型 Options）
- **Scriban** 模板引擎 → 生成 Markdown + HTML 报告
- 指标计算：纯 C#，无三方依赖
- 运行形态：**GitHub Actions cron**（非本地计划任务），报告 `git commit` 回仓库

## 解决方案结构

```
AiInvestmentTraceAgent.sln
├── src/
│   ├── Agent.Console/         # 入口、编排、appsettings.json、DI 组装
│   ├── Agent.Core/            # 领域模型、指标计算、报告编排、接口定义
│   ├── Agent.DataSource/      # 行情/公告/财报/新闻 HttpClient 封装 + 解析
│   ├── Agent.Reporting/       # Scriban 模板 → Markdown/HTML
│   ├── Agent.Delivery/        # IReportDelivery：File(默认) / 企业微信(预留)
│   └── Agent.Summarizer/      # ISummarizer：NoOp(默认) / LLM(预留)
└── tests/
    └── Agent.Core.Tests/      # 及各模块对应测试工程
```

**依赖方向**：`Console → Core ← DataSource/Reporting/Delivery/Summarizer`。`Core` 定义接口，不依赖具体实现；外层实现 `Core` 的接口，由 `Console` 做 DI 组装。

## 功能范围（本期）

- ✅ 行情追踪（现价、当前市值等基础行情；不含技术指标/量比/换手率/涨跌幅展示）
- ✅ **持仓与买卖点（v1.1）**：报告展示持股份数、成本价、持股比例、当前市值、年内涨幅；按配置的总市值阈值判定"理想买点/1年内卖点"，命中买点显示"可以击球"红色高亮。详见 `docs/requirements.md` §5.6。
- ✅ 公告追踪：重要类型过滤，纯规则
- ✅ 财报追踪（A股）：关键财务数据（营收/净利/同比/ROE/毛利/EPS）+ 披露提醒 + 业绩预告；港股财报本期不做（东财无 HK 财务数据，HKEX 财报为 PDF）
- ✅ 风险消息：关键词命中高亮，纯规则、不解读内容
- ✅ **报告分组显示（v1.1）**：按 `Group` 分区（核心持仓在前、观察池在后），两类卡片字段不同（观察池精简，无持仓/盈亏）。
- ✅ 本地文件输出（移动优先单列卡片；响应式 HTML + Markdown + 预留 `WeChatHtml` 公众号内联样式导出）→ Actions 提交回仓库

**明确不做**：AI 型买卖决策建议/理由解读（买卖点仅纯总市值阈值机械提示）、盘中实时监控。**技术/短期指标与异动提醒（v1.1 移除）**：涨跌幅、量比、换手率、MA5/20/60、近 5/20 日涨跌幅、主力净流入，以及基于涨跌幅/量比的异动阈值提醒（含 `Alerts` 配置与异动提醒区）均不做。

## 关键决策（不要擅自更改，需先与用户确认）

- **AI 层默认关闭**：`ISummarizer` 预留但默认 `NoOpSummarizer`。本期所有功能为纯规则/关键词，**零大模型成本**。启用 AI（读懂公告/风险分级）需显式开启，届时用 `claude-haiku-4-5`。
- **数据源**：东方财富 push2（行情主源，含 `f116` 总市值）、新浪（备源）、东财 K 线（仅供年内涨幅取年初收盘）、东财外汇（HKD/CNY 汇率，失败回退静态）、**东财统一公告 API（A股公告，np-anotice-stock）**、**港交所披露易 hkexnews（港股公告，两步：prefix.do 查 stockId → titleSearchServlet 检索）**、东财财务接口（财报）。
  - 注：A股公告原定巨潮 cninfo，实测东财统一公告 API 更简洁一致且海外可达，已改用东财。公告按市场路由：A股用中文重要类型包含匹配，港股（英文标题）用例行件排除过滤。
- **调度（v1.1 改周跟踪，已与用户确认）**：GitHub Actions，**每周五 13:00 UTC（北京周五 21:00，晚 9 点）触发一次**（`cron: "0 13 * * 5"`）。周频**不做交易日判断**，周五无论是否休市都照常跑（休市则取到最近交易日收盘快照）；原每日"交易日跳过"逻辑（m7）在周频下停用。公告/财报/风险内容窗口改为**近 7 天（本周）**。报告文件名用当周周五日期。
- **投递**：默认写文件；企业微信/Server酱推送为预留接口。微信公众号群发本期走**人工复制粘贴** `WeChatHtml` 导出，公众号 API 自动发布（需认证服务号+审核）列后续 `IReportDelivery` 实现。
- **报告布局与公众号导出（v1.1，已与用户确认）**：报告按 `Group` 分区显示（核心持仓/观察池分开，观察池卡片精简、无持仓与盈亏字段；持股比例分母只算有持仓的股票）。**移动优先单列卡片**，不用宽表格。HTML 策略：**保留带 `<style>` 的响应式 HTML 为主输出，另加 `WeChatHtml` 公众号导出格式**——全内联样式、无 `<style>`/`<script>`/`<head>`，可直接粘贴进公众号编辑器；由 `Report.Formats` 控制是否产出，默认不生成（预留启用）。
- **持仓与买卖点口径（v1.1，已与用户确认）**：v1.0 原将"持仓盈亏/买卖建议"列为不做，v1.1 放开为**持仓展示 + 规则型买卖点**。
  - 买卖点全部走**公司总市值**口径（现价 × 总股本，取自东财 push2 `f116`，单位统一**亿元人民币**）。
  - **理想买点/1年内卖点均来自配置**（`IdealBuyMarketCapYi` / `SellMarketCapYi`，是目标总市值，不是单股价格）。持股份数 `Shares`、成本价 `CostPrice` 亦来自配置，观察池可空。
  - **击球**：当前总市值(折人民币) < 理想买点 → 红色"可以击球"；**卖点**：当前总市值 > 卖点。只要配了买点，观察池股票也参与买点判定。
  - **持股比例** = 该股持仓市值(现价×份数，折人民币) ÷ 全部持仓总市值；**年内涨幅**基准为年初首个交易日收盘（K 线回溯需加长覆盖年初）。
  - **收益指标（组合概览两项 + 卡片一项）**：`年度收益(年内YTD)` = Σ(现价−年初收盘)×份数，率÷年初持仓总市值；`持仓总收益(对成本)` = Σ(现价−成本价)×份数，率÷总成本；核心持仓卡片另显示每股`对成本盈亏`。金额均折人民币汇总，**收益率按原币种算不受汇率影响**；缺年初收盘/成本价的持仓分别不计入对应汇总。
  - **证券类型（支持 ETF）**：`StockConfig.SecurityType`（`Stock`/`Etf`，不配则按代码前缀识别——沪 51/56/58、深 15/16 为 ETF）。**股票**买卖点走总市值口径；**ETF** 的 `f116` 是基金规模(AUM)非估值，改走**价格口径**（`IdealBuyPrice`/`SellPrice`，现价比较），卡片"当前市值"标签改"基金规模"、ETF 无财报。识别在 `SecurityTypeResolver`、判定分支在 `HoldingCalculator`；其余（持仓市值/比例/YTD/对成本盈亏）与股票同口径复用。
- **跨市场汇率换算（v1.1，已与用户确认）**：港股金额按 **HKD→CNY** 折算，封装 `Core.ICurrencyConverter`。汇率**实时抓取（东财外汇 HKD/CNY）+ 失败回退 `Currency.StaticRates` 静态汇率**。折算作用于市值类字段/持股比例/组合汇总/买卖点判定（统一人民币口径）；**每股价格（现价/成本价）保留原币种**（港股 HK$），报告标注币种并在汇总处脚注汇率与时间。
- **移除技术/短期指标（v1.1，已与用户确认）**：彻底移除涨跌幅、量比、换手率、MA5/20/60、近 5/20 日涨跌幅、主力净流入；**异动阈值提醒**功能一并删除（删指标计算、`AlertEvaluator`/`AlertOptions`、`Alerts` 配置、资金流数据源 `EastMoneyFundFlow*`/`IFundFlowSource` 及相关测试与报告区块）。K 线源保留但仅用于取年初收盘价算年内涨幅。

## 编码约定

- 配置与逻辑分离，**无硬编码本地路径**（便于将来迁云）。
- 所有网络请求：超时 + 重试 + 备用源降级 + 结构化日志。
- 数据源接口变更/反爬风险高 → 解析层容错，字段缺失不崩溃，记录并跳过。
- 股票代码统一格式 `600519.SH` / `00700.HK`，在边界处解析市场与纯代码。
- 命名清晰；仅在意图不明显处加注释；生产路径必须有错误处理。
- 绝不在代码/示例中写入真实密钥或凭证，用占位符；Webhook/API Key 走 GitHub Secrets。

## 常用命令

```bash
dotnet test                              # 跑全部测试（提交前必过）
dotnet test --filter FullyQualifiedName~HoldingCalculator   # 跑指定测试
dotnet test --filter "Category!=Integration"          # 跳过打真实接口的集成测试
dotnet run --project src/Agent.Console   # 本地生成一次报告
dotnet build                             # 构建
```

## 已知风险 / 首要验证

- **海外 GitHub runner 访问中国行情/公告接口可能限流或失败**。
- 👉 **实现第一步**：先做最小连通性验证——一个只取 1 只 A股 + 1 只港股行情/公告的小程序，在 Actions 上实跑确认能取到数据，再搭完整框架。避免搭好一切才发现云端取不到数。
